import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alleyqrat-riders',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
